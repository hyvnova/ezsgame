from main import *
import random

# INIT --------------------------------------------------------------------------------------------------------------
s = Screen(title="Test", fps=60, show_fps=True, size=[480, 400])

# START ------------------------------------------------------------------------------------------------------------- 
c = Circle(pos=["center", "center"], radius=20, color="red")
Slide(s, c, start=["center","top"], end=["center", "bottom"], step=1, time=30, loop=True)

# TIME & EVETS ---------------------------------------------------------------------------------------------------------------

while True:
    # IN-LOOP EVENTS ---------------------------------------------------------------------------------------------------------
    s.check(s)

    # BASE ------------------------------------------------------------------------------
    s.fill(color['black'])

    # LOGIC ----------------------------------------------------------------------------

    # DRAW ------------------------------------------------------------------------------   
    c.draw(s)
    
    # UPDATE ----------------------------------------------------------------------------
    s.update()

                